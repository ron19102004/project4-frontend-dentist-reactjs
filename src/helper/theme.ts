// theme.ts
import { createTheme } from '@mui/material/styles';

// Tạo theme tùy chỉnh với font
const theme = createTheme({
  typography: {
    fontFamily: "poppins:", // Đặt font của bạn ở đây
  },
});

export default theme;
